function multiply(a, b) {
    console.log(a * b);
  }
  
  // Cannot be done
  // console.log(a);
  multiply(5, 10);
  multiply(3, 4);
  
  function printName(firstName, lastName) {
    console.log(firstName + " " + lastName);
  }
  
  printName("Shelly", "Dimmer");
  printName("John", "Smith");